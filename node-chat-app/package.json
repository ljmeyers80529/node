{
    "name": "chat-app",
    "version": "1.0.0",
    "description": "",
    "main": "index.js",
    "scripts": {
        "start": "node server/server.js",
        "testx": "export NODE_ENV=test || SET \"NODE_ENV=test\" && mocha server/**/*.test.js --timeout=45000",
        "test": "mocha server/**/*.test.js --timeout=45000",
        "test-watch": "nodemon --exec \"npm test -- --timeout=45000\""
    },
    "author": "",
    "license": "ISC",
    "dependencies": {
        "express": "^4.16.3",
        "moment": "^2.22.2",
        "socket.io": "^2.1.1"
    },
    "devDependencies": {
        "expect": "^23.1.0",
        "mocha": "^5.2.0",
        "nodemon": "^1.17.5"
    }
}
